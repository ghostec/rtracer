#include "catch.hpp"
#include "math/float64.hpp"

TEST_CASE("operator==") {
  CHECK(true);
  // CHECK(rcanvas::equals(0.5, 0.4) == false);
  // CHECK(rcanvas::equals(0.5, 0.5) == true);
  // CHECK(rcanvas::equals(0.50000006, 0.50000006) == true);
  // CHECK(rcanvas::equals(0.50000006, 0.50000005) == false);
  // CHECK(rcanvas::equals(100, 99) == false);
  // CHECK(rcanvas::equals(100, 100) == true);
  // CHECK(rcanvas::equals(100.001, 100.001) == true);
  // CHECK(rcanvas::equals(100.001, 100.002) == false);
  // CHECK(rcanvas::equals(10000000.001, 10000000.002) == false);
  // CHECK(rcanvas::equals(10000000.001, 10000000.001) == true);
}
